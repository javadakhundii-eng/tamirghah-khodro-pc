# ساخت فایل EXE — حساب‌داری تعمیرگاه (ویندوز)

## پیش‌نیاز
Node.js از `https://nodejs.org` (نسخه LTS) — روی خود ویندوز نصبش کن.

## مراحل (توی پوشه `tamirgah-desktop`، با Command Prompt یا PowerShell)

```bash
npm install
npm run build:win
```

بعد از چند دقیقه، فایل‌های خروجی این‌جا ساخته می‌شن:
```
dist/حسابداری تعمیرگاه 1.0.0.exe        <- نسخه Portable (بدون نصب، فقط دوبار کلیک)
dist/حسابداری تعمیرگاه Setup 1.0.0.exe  <- نسخه نصب‌شونده معمولی (فایل ست‌آپ)
```

هرکدوم رو خواستی می‌تونی به هر ویندوزی منتقل کنی و اجرا کنی — نیازی به نصب Node.js روی سیستم مقصد نیست.

## تست سریع بدون ساخت exe

اگه فقط می‌خوای سریع تست کنی بدون ساخت فایل نهایی:
```bash
npm install
npm start
```
اپ توی یه پنجره دسکتاپ باز می‌شه.

## نکته مهم

این یه **پروتوتایپ تست UI/UX** هست، دقیقاً همون چیزی که روی گوشی (نسخه اندروید) هم داری — کد و منطقش یکسانه، فقط پنجره‌اش عوض می‌شه. داده‌ها فقط توی حافظه اجرا (RAM) می‌مونن؛ با بستن برنامه، اطلاعات پاک می‌شن (چون هنوز به دیتابیس واقعی وصل نیست).
