# RepairShop Pro — راهنمای اجرا و ساخت نسخه ویندوز

RepairShop Pro یک برنامه آفلاین تک‌کاربره است. اطلاعات در SQLite داخل پوشه داده‌های کاربر ویندوز نگهداری می‌شود و برای کار روزمره به اینترنت یا سرور نیاز ندارد.

## اجرای محیط توسعه

```bash
npm install
npm run dev
```

## بررسی و ساخت رابط کاربری

```bash
npm run check
npm run build
```

فایل‌های آماده Electron در `frontend/dist` تولید می‌شوند. فونت Vazirmatn نیز همراه برنامه بسته‌بندی می‌شود؛ بنابراین رابط فارسی به اینترنت وابسته نیست.

## ساخت نصب‌کننده ویندوز

این دستور باید روی ویندوز (یا محیط CI دارای Wine) اجرا شود:

```bash
npm run build:win
```

فایل خروجی نصب با نام زیر در `dist` قرار می‌گیرد:

```text
RepairShop Pro Setup.exe
```

## داده و پشتیبان‌گیری

- دیتابیس `repairshop-pro.sqlite` در مسیر داده‌های برنامه Electron ساخته می‌شود.
- از گزینه «پشتیبان‌گیری امن» در نوار کناری برای کپی امن فایل SQLite استفاده کنید.
- پوشه‌های `storage/` و `reports/` برای توسعه ذخیره عکس خودرو و گزارش/فاکتور در نظر گرفته شده‌اند.
